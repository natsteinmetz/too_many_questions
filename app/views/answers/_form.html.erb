<%= form_for [@question, @answer] do |f| %>
	<%= f.error_messages %>
	<p>
		<%= f.label :description %><br>
		<%= f.text_area :description %>
	</p> 
	<p>
		<%= f.label :date %><br>
		<%= f.date_select :date %>
	</p>
	<%= f.submit %>
<% end %>
